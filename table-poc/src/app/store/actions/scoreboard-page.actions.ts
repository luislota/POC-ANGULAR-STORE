import { createAction, props } from '@ngrx/store';
export const applyColumns = createAction('[applyColumns]');
export const dataAPI = createAction('[dataAPI]');
export const dataAPISuccess = createAction('[dataAPISuccess]');
export const dataAPIFailed = createAction('[dataAPIFailed]');

